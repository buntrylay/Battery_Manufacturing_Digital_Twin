apiVersion: 1
 
datasources:
  - name: Postgres
    type: postgres
    access: proxy
    url: postgres_db:5432
    user: postgres
    database: postgres
    secureJsonData:
      password: "password"
    jsonData:
      sslmode: "disable"
      timescaledb: false